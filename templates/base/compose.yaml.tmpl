# DO NOT EDIT — generated by deckhand
services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace
{{- if .ExposedPorts }}
    ports:
{{- range .ExposedPorts }}
      - "127.0.0.1:{{ .Port }}:{{ .Port }}"
{{- end }}
{{- end }}
    command: sleep infinity
