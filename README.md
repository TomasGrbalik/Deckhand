# Deckhand

A CLI tool for orchestrating Docker-based development environments on remote servers.

Deckhand manages the full lifecycle of containerized dev environments — from spinning up containers with the right language tools and services, to managing port mappings and generating SSH tunnel commands for thin-client access.

## Status

Early development. See `design/` for architecture and planning docs.
